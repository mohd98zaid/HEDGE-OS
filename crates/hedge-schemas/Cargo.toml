[package]
name = "hedge-schemas"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "FlatBuffers and JSON schemas shared across the PROJECT HEDGE workspace."
build = "build.rs"

[lib]
path = "src/lib.rs"

[dependencies]
flatbuffers = { workspace = true }
rkyv = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
bytes = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }

[build-dependencies]
# build.rs uses only std; no external crates required.
