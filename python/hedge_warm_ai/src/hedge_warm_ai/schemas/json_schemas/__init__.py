"""Canonical JSON schemas. Loaded as package data via ``importlib.resources``.

The .schema.json files in this directory are mirrored from
``crates/hedge-schemas/json_schemas/``. Treat the Rust copy as the source of
truth: any change should land in both directories together.
"""
